$(document).ready(function() {
function renderPage() {
    $(".content-mails").html("<%= j render 'layouts/content', id: @email.id %>");
    $("#<%= @email.id %>").attr('class','show list-group-item list-group-item-action read');
};
renderPage();
});
